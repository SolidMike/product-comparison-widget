import React from 'react';
import iFilters from '../../App'

const ProductComparsion: React.FC = ({filters: iFilters, products: iProducts}) => {
    return (
        <div>
            
        </div>
    );
};

export default ProductComparsion;
